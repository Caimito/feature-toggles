feature-toggles:
  toggles:
    foo:
      enabled: true
    new-dashboard:
      enabled: true
      roles: ["ADMIN", "USER"]
      experimentPercentage: 50
    

logging:
  level:
    # org.springframework: DEBUG
    net.caimito.features: DEBUG